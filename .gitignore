# ── Python ──
__pycache__/
*.py[cod]
*.egg-info/
.env
.venv/
venv/
.ipynb_checkpoints/

# ── 대용량 데이터 (GitHub 제외) ──
data/yolo_dataset/
data/*.zip
# estimates.db(112MB)는 GitHub 100MB 제한 초과 → HF Hub 데이터셋으로 배포.
# rag_db·knowledge PDF는 용량이 작아(~11MB씩) 직접 커밋함.
data/*.db

# ── 모델 가중치 ──
models/*.pt
models/*.onnx
models/car_damage_seg/
yolov8n-seg.pt
yolov8n.pt
runs/

# ── 배포 불필요 파일 ──
발표대본.md
train_colab.ipynb
train_yolo_notebook.ipynb

# ── 시스템/에디터 ──
.DS_Store
*.log
.streamlit/secrets.toml
.streamlit/secrets.tom
